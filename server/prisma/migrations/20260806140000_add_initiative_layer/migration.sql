-- الجبهة أ (الجلسة ٢): طبقة الفجوة على المبادرة — رأس التقرير المصدَّر.
-- بُعدٌ مستقلّ عن priority (النوع لا الإلحاح). اختياريّ بلا افتراض → البيانات
-- القديمة تبقى، صفر مخاطرة. القيم: 'mandatory' | 'structural' | 'improvement'.
ALTER TABLE "Initiative" ADD COLUMN "layer" TEXT;
