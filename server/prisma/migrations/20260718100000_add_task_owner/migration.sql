-- الجهة المنفّذة (owner): نصّ حرّ لجهة تنفّذ المهمة يشرف عليها المدير المستقل
-- (شركة/إدارة/شخص) — بلا حساب مستخدم. اختياري (String?) → البيانات القديمة
-- تبقى، صفر مخاطرة، وقابل للعكس بـ DROP COLUMN.
ALTER TABLE "Task" ADD COLUMN "owner" TEXT;
