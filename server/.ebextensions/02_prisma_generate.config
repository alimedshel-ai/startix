# Prisma needs `prisma generate` after deploy so the client binary matches the
# Linux runtime — local generates on macOS/Windows produce the wrong binary.
container_commands:
  01_prisma_generate:
    command: "npx prisma generate"
    leader_only: true
