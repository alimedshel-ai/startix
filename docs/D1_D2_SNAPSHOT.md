# لقطة سلوكيّة D1+D2 — قياسٌ على قاعدة البيانات الحيّة (بعد القلب)

**المنصّة:** ستارتكس · **التاريخ:** 2026-08-05 · **النوع:** دليلٌ بصريّ فوق دليل الاختبار
> المسار أ (النصيّ): تيّار قراءةٍ بحت على ٢٤ عميلاً حقيقيّاً — لا يكتب شيئاً.
> السكربت: `server/src/scripts/d1d2Snapshot.ts` — يُعاد تشغيله بـ`ts-node` للتحقّق.

## الأمر
```
cd server && npx ts-node --transpile-only src/scripts/d1d2Snapshot.ts
```

## المخرَج الخام (مُلصَق)
```
=== الأرض: 174 مرفقاً استراتيجيّاً عبر 24 عميلاً ===

─── D1 · انحراف «الوجود مقابل المحتوى» (مرفقٌ حاضرٌ فارغ) ───
    القديم يعدّه «مكتملاً ✅» · المحرّك الجديد يعدّه «لم يبدأ ⬜ ويوجّه لملئه»

    • walk-5-need-initiatives × INTERNAL_ENV  =  {}
    • walk-5-need-initiatives × PESTEL  =  {}  ⟵ مصدر SWOT خارجيّ (يقلب بوّابة D2)
    • walk-4-need-swot × INTERNAL_ENV  =  {}
    • walk-4-need-swot × PESTEL  =  {}  ⟵ مصدر SWOT خارجيّ (يقلب بوّابة D2)
    • walk-3-need-pestel × INTERNAL_ENV  =  {}

─── D2 · بوّابة SWOT ثنائيّة المصدر لكل عميل ───
    externalReady = وجود أحد PESTEL/PORTER/BENCHMARK/STAKEHOLDERS بمحتوى

    ✗ walk-5-need-initiatives: خارجيّ حاضر (PESTEL) لكنه فارغ
        القديم: «SWOT مفتوح → اذهب إليه» · المحرّك: «أكمل PESTEL أوّلاً»
    ✗ walk-4-need-swot: خارجيّ حاضر (PESTEL) لكنه فارغ
        القديم: «SWOT مفتوح → اذهب إليه» · المحرّك: «أكمل PESTEL أوّلاً»

=== الخلاصة: D1 فوارغ=5 · D2 عملاء بوّابة منحرفة=2 ===
```

## القراءة
- **D1:** ٥ مرفقاتٍ حاضرةٌ فارغة `{}` على ٣ عملاء. القاعدة القديمة (الوجود) تعدّها مكتملة
  فتترك حفرةً؛ المحرّك (المحتوى، `deepHasContent`) يعدّها «لم يبدأ» ويوجّه لملئها.
- **D2:** عميلان لهما PESTEL حاضرٌ فارغ. القاعدة القديمة «الخارجيّ موجود ⇒ SWOT مفتوح»
  ترميهما على باب مقفل؛ المحرّك (`externalSourceReady` واعٍ بالمحتوى) يوجّههما لإكمال PESTEL.

## الربط بالإثبات الكوديّ
- الانحراف **مُثبَتٌ على بيانات حقيقيّة** (أعلاه) + **باختبار وحدة**:
  `journeyShadow.test.ts` (٤/٤) لـD1 · `cockpitLeadShadow.test.ts` (٢/٢) لـD2.
- السكربت يُطابق مُحدِّدَي المحرّك حرفيّاً: `deepHasContent` (artifactContent.ts:9) +
  `EXTERNAL_SWOT_BASES` (useGuidedNext.ts:37) — لا منطق موازٍ جديد.
