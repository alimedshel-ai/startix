# CONSUMPTION_GAPS.md — فجوات طبقة الاستهلاك

**التاريخ:** 2026-08-01 · **الحالة:** مسودّة — تُودَع (commit) في `docs/` **بعد نقطة حفظ الـ٢٤**، لا قبلها.
**الغرض:** كل بند = **منتِج · مستهلك · الفجوة · البوّابة**. لطبقة محرّك ok-computer دفعةً واحدة بعد التجميد.
**القاعدة:** لا بند يُنفَّذ قبل بوّابته. البناء فوق بوّابة غير محسومة = ما تجنّبناه طوال السلسلة.

---

## C1 — حارس التقدّم (قرار ب)
- **المنتِج:** أدوات العمق (سلسلة القيمة · بورتر · DNA) في خطّة التحليل.
- **المستهلك:** `useGuidedNext.ts §1.5` (مشي الأدوات).
- **الفجوة:** يفرض كل أداة عمق موصى بها **بلا حارس تقدّم** → يردّ المستخدم من ⑥ (التنفيذ) إلى ① (حلقة سلسلة القيمة).
- **البوّابة:** هدوء ok-computer (`useGuidedNext` ملفّه الساخن).
- **الشرطان:** البطاقة الموجّهة فقط (لا بوّابات الإكمال) · تلميح «عمّق تحليلك» يبقى ظاهراً بعد التوليف.

## C2 — P2: اكتمال ① واعٍ بالمحتوى (جزآن لا ينفصلان)
- **المنتِج:** artifact `MATURITY` (تقييم النضج لـHR/FINANCE).
- **المستهلك:** `journeyStages.completionArtifacts[environment]` + `useJourneyCompletions.ts:73`.
- **الفجوة:** (أ) `MATURITY` غائب عن `journeyStages` ① → المحرّك يراه، السايدبار لا. (ب) سطر ٧٣ يقيس بالوجود (`artifactTypes`) لا المحتوى → **إضافة MATURITY وحدها تُحيي يتيم `{}` الفارغ.**
- **البوّابة:** **الجزآن معاً إلزاميّان** (النصف يُحيي اليتيم — شرط ٢) · هدوء ok-computer (`useJourneyCompletions` ساخن).
- **الملفّان:** `journeyStages.ts` (بارد) + `useJourneyCompletions.ts` (ملك ok-c ساخن).
- **اختبار القبول:** HR بـMATURITY **غير فارغ** → ① مكتملة (سايدبار+محرّك) · MATURITY **فارغ** → ① غير مكتملة.

## C3 — طيّ محاور MaturityInApp الخمسة
- **المنتِج:** `config.sections` (٥ محاور نضج HR).
- **المستهلك:** `MaturityInApp` (العرض).
- **الفجوة:** الأقسام تُعرض كلّها مفتوحة (قائمة طويلة) — طلب المالك بطيّها.
- **البوّابة:** ملكيّة `MaturityInApp` (ملك ok-c ساخن — e0ea6f5/0a3fcc7). النمط **جاهز ومُثبَت** (كوميت `9fa5611` على DeepAnalysisPage).
- **الملف:** `MaturityInApp.tsx` (ملك ok-c).

## C4 — جسر archetype↔level (اتساق عرض · غير عاجل)
- **المنتِج:** تصنيفان — `strategicPath` (EMERGENCY_RISK…) · `classify` (emergency/foundation…).
- **المستهلك:** بطاقة المسار (١٢ مستهلكاً) · بيكون useGuidedNext.
- **الفجوة:** لا جسر → قد يرى المستخدم «مساراً» في البطاقة و«مستوى» مختلفاً في البيكون لو تباعدا.
- **البوّابة:** ملاحظة اتساق، لا عطل عاجل. الملفّان: `strategicPath.ts` · `classify.ts`.

## النصفان المتشابكان (من الطبقة B الملغاة — ملك ok-c، يُكمله)
- **`nav.ts`:** حذف قسم «💼 لوحات القيادة» (أ٣) — تعديلي متشابك مع عمل ok-c غير المُلتزَم. نصفي (التبويبات) على الأساس (`ae9a9ef`). **البوّابة:** ok-c يملك nav.ts الطائر → يُكمل الحذف.
- **`OnboardingPage.tsx`:** شريحة هوية الشركة (onboarding) — متشابكة. نصفي (الخلفيّة: server/auth + authStore) على الأساس (`c9acea5`). **البوّابة:** ok-c يملكه → يُكمل الشريحة.

## ★ RESCUE_CHALLENGES — ملكيّة متنازعة + عطل درفت مؤكّد (يُحسم قبل أي تنفيذ)
- **الوضع:** ok-computer يبني حزمة الإنقاذ التشاركيّ — `0b6ce9f` (commit ①: النوع) · `2252a7e` (commit ②: خطوة التحديات الاختياريّة). **كانت commit ① مُخطَّطة لي.** الحزمة صارت ملكه فعلاً.
- **🔴 عطل مؤكّد (صنف HR_QUANT):** `RESCUE_CHALLENGES` في `ArtifactType` بالعميل (`strategicApi.ts:59`) لكن **غائب عن allowlist السيرفر** (`server/src/controllers/strategic.ts` `ARTIFACT_TYPES`) → **حفظه سيرجع 400 «طلب غير صالح»**. **الإصلاح:** إضافته إلى ARTIFACT_TYPES بالسيرفر (سطر واحد) — يملكه من يملك حزمة الإنقاذ.
- **البوّابة:** **تُحسم الملكيّة قبل أي تنفيذ.** لا تبدأ حزمة الإنقاذ — ok-computer يبنيها. أبلِغه بعطل الدرفت السيرفريّ فوراً (يكسر الحفظ حتى لو اكتملت الواجهة).

---

## خريطة البوّابات (ملخّص)
| البند | الملف(ات) | البوّابة | الحالة |
|---|---|---|---|
| C1 حارس التقدّم | useGuidedNext (ساخن) | هدوء ok-c | مؤكّد |
| C2 اكتمال ① محتوى | journeyStages (بارد) + useJourneyCompletions (ساخن) | الجزآن معاً · هدوء ok-c | مؤكّد |
| C3 طيّ المحاور | MaturityInApp (ساخن) | ملكيّة ok-c · النمط جاهز | طلب مالك |
| C4 جسر archetype↔level | strategicPath + classify | اتساق، غير عاجل | ملاحظة |
| المتشابكان | nav.ts + OnboardingPage (طائران ok-c) | ok-c يُكمل | نصفي على الأساس |
| ★ RESCUE_CHALLENGES | strategicApi (عميل ✓) + strategic.ts (سيرفر ✗) | حسم الملكيّة + إصلاح الدرفت | 🔴 عطل + تنازع |
