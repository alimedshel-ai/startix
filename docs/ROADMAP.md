# خارطة عمل توحيد محرّك «الخطوة التالية»

> آخر تحديث: 2026-07-25 · الفرع: `nav/guided-path`
> **مهم:** رقع الدلتا A–F **مُنجَزة ومُلتزَمة** (٣ كوميتات). هذا الملف يوثّق ما **تبقّى فعلاً** — لا يُعيد إنتاج ما تمّ.

---

## ✅ المُنجَز (مُلتزَم على `nav/guided-path`)
| الكوميت | المحتوى |
|---|---|
| `b10b478` | الدلتا A–E: حذف المحرّك الموازي (ESSENTIAL_SEQUENCE) · ⭐ من useGuidedNext · المبسّط إحماء ◇ · Initiative.source/linkedActionId + شارة «🚨 من الإنقاذ» · ترحيل إنقاذ القمرة (resolveRescuePlan خلف flag) + إنشاء inline · جدول journey_events (مخطّط) · سطور الملكيّة (تعليقات كود) |
| `36a1dbe` | الرقعة F: نقل stageStatus للمحرّك · حارس الترتيب · حذف كود locked الميّت · إصلاحات المراجعة |
| `e9c0c30` | تنطيق useCompany (useCompanyById) · maturity-completes-deep · تسمية عمق التحليل مختصر/موسّع/شامل (المرحلة أ) |

**الحالة:** 152 اختبار يمرّ · tsc+eslint نظيف · الأعلام OFF افتراضاً · هجرتا التطوير مُطبَّقتان.

---

## 🟥 المسار الحرج — إغلاق التوحيد (تسلسل إلزاميّ)
| # | البند | معيار النجاح | الحجم | يعتمد |
|---|---|---|---|---|
| ١ | دمج PR (`phase-6-lifecycle` ← `nav/guided-path`) | PR مدموج | دقائق | — |
| ٢ | تفعيل الأعلام تجريبياً (`USE_JOURNEY_NEXT`+`USE_RESCUE_PLAN`=1) ثم تحقّق بصريّ | القمرة=السايد بار=لوحة العميل تعرض نفس التالي لنفس العميل | ساعة | ١ |
| ٣ | **كاتب أحداث D** — endpoint `journey_events` + كاتب في useGuidedNext (`next_shown` عند العرض · `accepted`/`overridden` عند النقر) **+ ترحيل الإنتاج معه** | كل عرض يرسل next_shown؛ كل نقرة accepted/overridden | نصف يوم | ٢ |
| ٤ | **حذف القديم** — `RESCUE_SEQUENCE`·`getRescueNext`·`StartHereBeacon` + الأعلام | `grep "getRescueNext\|StartHereBeacon" client/src` = صفر · الأسطح متطابقة | نصف يوم | ٢ نجح |

> **قاعدة الأمان (الخميس):** لا تحذف إلا إذا grep=صفر **و** الأسطح متطابقة. لو تعارض — قف.

> **تحديث ٢٠٢٦-٠٧-٢٩ — تحقّق ساكن للبند ٢ (الـDB محجوب P1001، فلا تحقّق حيّ):**
> - ✅ **تطابق العرض:** الأسطح الثلاثة تقرأ `next.{label,icon,reason,to}` مباشرةً بلا تحوير.
> - 🐞 **عطل مصدر-واحد اكتُشِف وأُصلِح:** على القمرة (`/clients/:id/run`) كان السايد بار
>   يفقد العميل (ريجيكسه `…/([^/]+)$` لا يلتقط المسار الفرعيّ، والروابط لا تحمل
>   `?client=`) → `useGuidedNext(null)` بينما القمرة `useGuidedNext(id)`. البطاقة كانت
>   تقول «اختر عميلاً» والقمرة تعرض التالي الحقيقيّ. **الـDB كان سيُخفيه خلف «محجوب».**
>   الإصلاح: توسيع ريجيكس `Sidebar.tsx:100` ليلتقط `/run`·`/journey`. تحقّق: محاكاة ٦
>   مسارات · `tsc` نظيف · ٦٣ اختبار رحلة يمرّ. (`nextCard` غير مشروط بالوضع — سطر ٢٣٢.)
> - ⛔ **المتبقّي (حيّ فقط):** فرق بصريّ عند وجود بيانات — يحتاج DB + عميل مبذور. صغير:
>   الأسطح الآن تقرأ نفس `gn` لنفس العميل منطقيّاً. يُغلَق حين يعود الوصول للـDB.

---

## 🟧 توحيد أعمق — المُقرِّرات الموازية المتبقّية
| # | البند | التفصيل | الحجم |
|---|---|---|---|
| ٥ | ترحيل `NextStepCard` للمصدر الموحّد | مُقرِّر موازٍ + **خطأ**: `ANALYSIS_SEQUENCE.deep.deptPath='/manager/dept-deep'` (المبسّط) بدل `/manager/deep-analysis` → «التالي» يلفّ للخلف | يوم |
| ٦ | حسم تناقض ١.٥ ↔ ٢ | نفس العميل (تدقيق فقط) → «7S» عبر analysisPlan لكن «PESTEL» عبر getNextStep (التدقيق يُغني عن الداخليّ في ٢ لا ١.٥) — قرار توحيد تعريف «المصدر الداخليّ» | نصف يوم + قرار |

---

## 🟨 التسمية والتلميع
| # | البند | التفصيل | الحجم |
|---|---|---|---|
| ٧ | تسمية مسار الرحلة — المرحلة ب | مركزة `journeyPathLabel()` (مكرّرة inline ~١٦ ملف/٢٤ موضع: classify·Sidebar·٣ path files·ClientDetail·DeptDashboard·`/settings/path`·…) → قصير/متوسط/طويل + `dualLabel` + حارس 2ب | نصف يوم |
| ٨ | حرس `/manager/*` ضدّ المالك | المالك يصل لبوّابة تخصّص بدل إعادة توجيه | ساعة |
| ٩ | تنظيف تجميليّ | أوصاف analysisPlan.test.ts «تكتيكيّ» + حذف عملاء البذر `walk-*` من التطوير | ربع ساعة |

---

## 🟦 ديون منتج سابقة (مستقلّة — رتّبها بالقيمة)
| # | الدَّين | الأثر | الحجم |
|---|---|---|---|
| ١٠ | pickStrategicPath يتجاهل runway/trajectory/stage (يختار من healthPct فقط) | **أعلى قيمة** | متوسط |
| ١١ | prompts dept-deep لـPROJECTS+SALES فقط؛ ١١ إدارة → GENERIC | احتكاك أوّل مرّة | محتوى كبير |
| ١٢ | تعريف العميل الموحّد (مدخل مرحليّ؛ ~٦٠٪ موجود) | — | كبير مرحليّ |
| ١٣ | titleKey migration (تغيير cleanInitiativeTitle يكسر dedup) | صغير | مؤجّل |
| ١٤ | المالك↔المدير الداخليّ يتشاركان الأهداف (shared-vs-derive) | متوسط | مؤجّل |
| ١٥ | AI initiative-breakdown يحتاج `ANTHROPIC_API_KEY` (503) | إعداد | صغير |
| ١٦ | تشتّت الأدوات / مسار المستقل الواحد الواضح | كبير UX | كبير |

---

## 📐 ترتيب الموجات المقترَح
```
الموجة القادمة:  ١ دمج → ٢ تفعيل → ٣ كاتب D → ٤ حذف القديم     [إغلاق التوحيد]
الموجة +١:        ٥ NextStepCard → ٦ تناقض ١.٥↔٢               [آخر التوحيد]
الموجة +٢:        ٧ تسمية ب → ٨ حرس → ٩ تنظيف                  [تلميع]
موازٍ:            ١٠ runway (أعلى قيمة) → ١١ prompts → …        [ديون المنتج]
```

## مبدأ حاكم
تغييرات العرض/التوحيد لا تمسّ القيم الداخليّة (QUICK/MEDIUM/LONG · operational/tactical/strategic) — صفر ترحيل بيانات، صفر كسر API. الملكيّة: مسار الرحلة ← `classify` فقط · عمق التحليل ← `analysisPlan` فقط · لا اشتقاق ثالث.
