# المهمّة ٢ — إصلاح الهبوط (ساعة، معزول · PR مستقلّ)

> اقرأ `docs/CURRENT_STATE.md` أوّلاً. **لا تلمس `NextStepCard`** (المهمّة ٣).

## الخطوة ١: أكّد (مُنجَز مسبقاً — في CURRENT_STATE)
- ✅ `types/user.ts:2` → `managerType === 'INDEPENDENT_PRO'`
- ✅ `nav.ts` → دالّة `landingAfterOnboarding` (منطقها سليم: تخصّص→تدقيقه · بلا تخصّص→`homeFor`)
- ⛔ `SelectTypePage.tsx:73` → **لا مسار دخول مباشر لخطوة التخصّص** (يبدأ من `'role'`)

## الخطوة ٢: شغّل TRACE_LANDING (دقيقتان)
> اقرأ `docs/TRACE_LANDING.md` — سجّل دخول والصق `[TRACE:landing]`. الجذر يُحسَم بالتتبّع لا بالافتراض.

## الخطوة ٣: حسب النتيجة (ثلاثة فروع)

### الفرع أ — المتجر (`via: "تخطّي"`) — الأرجح
**الجذر:** `skip()` في `OnboardingPage.tsx` يستخدم `user!` من المتجر (قديم بلا تخصّص)، بينما مسار الحفظ يستخدم `data.user` الطازج.
**الإصلاح:** في `skip()` — مرّر `data.user` الطازج (أو أحدِث المتجر قبل التوجيه) بدل `user!`. **سطر واحد، لا توجيه لإعادة إدخال.**

### الفرع ب — API/التسجيل (`via: "حفظ"`)
**الجذر:** الـAPI لا يُرجع `specialtyDeptType` (أو لم يُلتقَط في التسجيل).
**الإصلاح:** أصلِح الالتقاط/الإرجاع في المصدر.

### الفرع ج — التخصّص موجود (`specialty: "HR"`)
**الإجراء:** **قف وأخبِر** — الجذر مختلف عمّا افترضنا.

> ملاحظة: توجيه المستقلّ «لخطوة التخصّص» **ممنوع الآن** (لا مسار مباشر). إن لزم لاحقاً → أضِف `?step=specialty` أوّلاً في PR منفصل — قف وأخبِر قبل الاختراع.

## الخطوة ٤: اختبر
- مستقلّ بلا تخصّص → **لا يسقط على `/manager/clients` صامتاً**
- مالك/داخلي بلا تخصّص → يكمل عبر `homeFor` (بلا تغيير)

## معايير القبول
- [ ] `grep -rn "\[TRACE:" client/src/` = **صفر** (احذف التتبّع بعد الإصلاح)
- [ ] **لا تُمَسّ `NextStepCard`**
- [ ] tsc/eslint نظيف + الاختبارات خضراء
