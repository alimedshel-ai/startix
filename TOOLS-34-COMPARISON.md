# مقارنة وتحليل — ربط الأدوات الـ34 بمسار المدير المستقل

> **الحالة:** ملف تحليل قبل التنفيذ. لا تعديلات كود مبنية على هذا الملف حتى الاعتماد.
> **التاريخ:** 2026-07-10.
> **المصدر:** جرد فعلي على `main` + ملف الاقتراح `ربط أدوات التحليل الـ34 بمسار المدير المستقل — النسخة المفصّلة`.

---

## القسم أ — الوضع الحالي (Ground Truth)

### أ.1 — جرد الأدوات الـ34

| # | الأداة (اسم الملف الجديد) | الحالة | المسار الحالي |
|---|---------------------------|--------|----------------|
| 1 | البيئة الداخلية | ❌ غير موجودة | — |
| 2 | سلسلة القيمة | ✅ موجودة | `/value-chain` |
| 3 | Porter Five Forces | ✅ موجودة | `/porter` |
| 4 | PESTEL | ✅ موجودة | `/pestel` + `/manager/dept-pestel` |
| 5 | القدرات الجوهرية | ✅ موجودة | `/core-capabilities` |
| 6 | المقارنة المعيارية | ✅ موجودة | `/benchmarking` |
| 7 | رحلة العميل | ❌ غير موجودة | — |
| 8 | DNA المنظمة | ✅ موجودة | `/org-dna` |
| 9 | أصحاب المصلحة | ✅ موجودة | `/stakeholders` |
| 10 | اختبار الضغط | ❌ غير موجودة | — |
| 11 | الجاهزية الرقمية | ❌ غير موجودة | — |
| 12 | SWOT | ✅ موجودة | `/swot` |
| 13 | TOWS | ✅ موجودة | `/tows` |
| 14 | التوجه الاستراتيجي | ✅ موجودة | `/directions` |
| 15 | نموذج الأعمال Canvas (BMC) | ❌ غير موجودة | — |
| 16 | تحليل الفجوات | ✅ موجودة | `/gap-analysis` + `/manager/dept-gap` |
| 17 | الآفاق الثلاثة | ✅ موجودة | `/three-horizons` |
| 18 | الخيارات الاستراتيجية | ✅ موجودة | `/choices` |
| 19 | BCG Matrix | ✅ موجودة | `/bcg` |
| 20 | Ansoff Matrix | ✅ موجودة | `/ansoff` |
| 21 | الأهداف BSC | ❌ غير موجودة | — |
| 22 | KPIs | ✅ موجودة | `/kpis` |
| 23 | OKRs | ✅ موجودة | `/okrs` |
| 24 | OGSM | ✅ موجودة | `/ogsm` |
| 25 | الخريطة الاستراتيجية | ❌ غير موجودة | — |
| 26 | المبادرات | ✅ موجودة | `/initiatives` |
| 27 | مصفوفة الأولويات | ✅ موجودة | `/priority-matrix` |
| 28 | خريطة المخاطر | ✅ موجودة | `/risk-map` |
| 29 | مختبر المحاكاة | ✅ موجودة | `/ai/simulation` |
| 30 | مصفوفة أيزنهاور | ❌ غير موجودة | — |
| 31 | RACI Matrix | ❌ غير موجودة | — |
| 32 | مركز الذكاء | ✅ موجودة | `/ai-center` |
| 33 | باريتو 80/20 | ❌ غير موجودة | — |
| 34 | مخطط جانت | ✅ موجودة | `/gantt-chart` |

**النتيجة الأولية:** `24 موجودة / 10 مفقودة`. الأدوات المفقودة العشر:
1. البيئة الداخلية · 7. رحلة العميل · 10. اختبار الضغط · 11. الجاهزية الرقمية · 15. BMC · 21. BSC · 25. الخريطة الاستراتيجية · 30. أيزنهاور · 31. RACI · 33. باريتو.

### أ.2 — حقول التسجيل الفعلية (`server/src/controllers/auth.ts`)

مقبولة الآن: `email, password, name, userType, managerType?, specialtyDeptType?, firstClientName?, phone?`

**غير موجودة:** `pains, goals, opex (team/budget/target/avgSalary), sector, subsector, entityType`.

### أ.3 — بنك الأسئلة الحالي

- **`client/src/lib/managerInvestorQuestions.ts`:** 7 حقول خفيفة للمدير (تُستخدم في `/preview/manager`).
- **`client/src/lib/deptQuestions.ts`:** 330 سؤالاً موزّعة على 13 إدارة × 6 أقسام (نُقلت من stratix legacy في الموجات السابقة). هذا هو البنك العميق.
- **الاقتراح الجديد يذكر:** "21 سؤالاً (3×7 إدارات)" — عدد أقل بكثير من الـ330 المتوفّرة.

### أ.4 — بنية الاستهلاك الحالية

- التسجيل → `/join` → `/onboarding` → `/manager/clients` (للمستقل).
- الأدوات مفتوحة عبر `?client=<id>` مع `useCompany` (يقرأ الـ query param).
- الأدوات في `ClientDetailPage` مجمّعة في 4 أقسام: أدوات العمل، التوليف الاستراتيجي، التخطيط والتنفيذ، التحليل المالي.
- **لا يوجد ترتيب مقفل (locked sequence)** — كل الأدوات مفتوحة دائماً للمدير المستقل.

### أ.5 — ملفات مرجعية

- `tools-data.json` و `platform-flowchart.html` **غير موجودَين** في هذا المشروع؛ هما في stratix legacy فقط.

---

## القسم ب — تحليل ما يقترحه الملف الجديد

الملف الجديد يطرح **طبقة ربط (Handoff + Goal-gating + OPEX feeding + Stage-locking)** بدل إعادة بناء الأدوات. عناصره الجوهرية:

1. **8 شرائح تسجيل غنيّة:** الدور → الإدارة → الهوية+OPEX → الآلام → الأهداف → الأسئلة التخصصية (21).
2. **Handoff:** بعد آخر سؤال، بدل شاشة نتيجة مغلقة، يُحقَن Payload كامل في التسلسل: `{ department, entity, sector, size, opex, pains[], goals[], deptAnswers[21] }`.
3. **5 مراحل مرتّبة، أربع مقفلة + مرحلتان مفتوحتان:**
   - ① البيئة الداخلية (تشخيص) → ② SWOT/TOWS → ③ التوجهات والخيارات → ④ المؤشرات.
   - ثم يُفتح: ⑤ المبادرات → ⑥ التنفيذ والمتابعة.
4. **بوّابة الأهداف:** اختيار الهدف (مثلاً "SWOT") يُشغّل مخرَجاته المرتبطة تلقائياً.
5. **تغذية OPEX:** المستهدف/الميزانية/الفريق تصل مباشرة إلى Gap/KPIs/Ansoff/RACI بدل إعادة سؤالها.

---

## القسم ج — التعارضات المحتملة مع الوضع القائم

| # | نقطة التعارض | الوضع الحالي | المقترح الجديد | الحلّ المُقترح |
|---|---------------|----------------|-------------------|-------------------|
| ج.1 | **عدد الإدارات** | 13 (HR, FIN, SALES, MKT, OPS, IT, CS, SUPPORT, LOGISTICS, QUALITY, PROJECTS, COMPLIANCE, GOVERNANCE) | 7 | نُبقي الـ13؛ نُطبّق الربط على كل ما هو موجود (المقترح يذكر 7 كتبسيط، لا كحصر). |
| ج.2 | **بنك الأسئلة** | 330 سؤالاً على 13×6 (`deptQuestions.ts` + `preview/manager-deep` جاهز في الخطة القائمة) | 21 (3×7) | نُبقي الـ330؛ نُضيف حقل تسجيل مخفّف اختياري إذا رغبنا. |
| ج.3 | **الخطة الحالية** `resilient-crafting-stardust.md` | تُنفَّذ حالياً الأمر ٢٧ (تحقّق الجزء ١) — الجزء ١ يُغطّي البنك العميق + `preview/manager-deep`. | يفترض الاقتراح غياب البنك | الاقتراح **مكمّل** للخطة القائمة، لا يبدلها. طبقة الربط تُنفَّذ **بعد** الأمر ٤٥. |
| ج.4 | **`/preview/manager` القديم (7 أسئلة)** | يبقى كما هو للتوافق العكسي | لم يُذكر | يبقى — الاقتراح لا يمسّه. |
| ج.5 | **تفكيك التسجيل إلى 8 شرائح** | حالياً TryDiagnosticPage → JoinPage → Onboarding (3 خطوات) | 8 شرائح | نمط `التوسيع التدريجي`: نضيف شرائح 3–5 (OPEX + Pains + Goals) قبل الأسئلة، بدون إعادة معمار الـ3 القائمة. |
| ج.6 | **10 أدوات مفقودة** | غير موجودة | مطلوبة كنقاط في التسلسل | إمّا نبنيها لاحقاً (Wave M+1)، أو نمرّرها كأدوات محلية بواجهات مبسّطة، أو نُخفيها من التسلسل مع علامة "قريباً". |
| ج.7 | **آلية القفل (`locked stages`)** | غير موجودة | ركن المقترح | تُبنى كطبقة UI فوق الأدوات القائمة (بدون تعديلها) — Bar تقدّم + guard صغير في الروتر أو الصفحات المفتاحية. |

**لا تعارض بنيويّ.** كل التعارضات قابلة للمصالحة بقرارات صغيرة (ترقيم، توسيع تدريجي، طبقة UI أعلى).

---

## القسم د — الفجوات الحقيقية (ما ينقص للربط)

| رقم | الفجوة | التأثير | الجهد التقديري |
|-----|--------|---------|-----------------|
| د.1 | 10 أدوات مفقودة | التسلسل المقترح يشير لأدوات لا وجود لها بعد | كبير — 10 صفحات جديدة أو stub-ات |
| د.2 | حقول تسجيل ناقصة (`pains`, `goals`, `opex`, `sector`, `entityType`) | لا يمكن Goal-gating ولا OPEX feeding بدونها | متوسّط — 3–4 شرائح جديدة + schema/زود + Prisma migration |
| د.3 | لا Handoff من التشخيص إلى التسلسل | التشخيص ينتهي عند "النتائج" بدل فتح ① | صغير — رابط + بذر بيانات في stores |
| د.4 | لا Goal-gating | كل الأدوات مفتوحة دائماً؛ اختيار الأهداف لا يوجّه | صغير — دالة نقيّة `visibleTools(goals[])` + قناع في الـ nav/ClientDetail |
| د.5 | لا OPEX feeding في الأدوات | كل أداة تسأل عن الأرقام من جديد | متوسّط — قراءة من `user` أو `company.metadata` داخل كل أداة |
| د.6 | لا Stage-locking | الترتيب المقصود لا يُفرض | صغير — component `<StageLock stage={n} prev={n-1}/>` |
| د.7 | لا `tools-data.json` مركزي | حالياً nav.ts + ClientDetailPage.tsx متفرّقان | صغير — استخراج بيانات الأدوات في ملف واحد يقود التسلسل |

---

## القسم هـ — القرار الأساسي قبل الخطة

قبل أي خطوة، نحتاج قراراً من صاحب المشروع على المحاور الأربع:

- **هـ.1 — أولوية الاقتراح:** هل نُنفّذ طبقة الربط **الآن** (وقف الجزء ٢ الحالي من `resilient-crafting-stardust.md` مؤقتاً) أم **بعد** إكمال الجزء ٢ (٢٨→٤٥)؟
- **هـ.2 — الأدوات العشر:** نبني كلها الآن، نبني الأربع الأساسية فقط (⭐ في المقترح: BMC + BSC + RACI + أيزنهاور)، أم نؤجّلها ونضع stub-ات؟
- **هـ.3 — حجم التسجيل:** نتوسّع لـ8 شرائح كاملة أم نبدأ بـ3 شرائح إضافية فقط (OPEX + Pains + Goals) نُدمجها في `/onboarding` الحالي؟
- **هـ.4 — عدد الإدارات:** نُبقي الـ13 (وضعنا الحالي) أم نقصر مسار المستقل على الـ7 التي يذكرها الاقتراح؟

---

## القسم و — الخطة المُقترحة مرتّبة (بعد اعتماد القرار)

> **ملاحظة:** الأرقام أدناه تفترض قرار «الربط بعد الجزء ٢» + «الأربع الأساسية الآن فقط» + «3 شرائح إضافية» + «الـ13 كما هي». يمكن تعديلها بحسب قرار هـ.

### المرحلة R1 — توسيع التسجيل (3 commits)
- R1.1: Prisma migration — إضافة `Company.opex` (team, budget, target, avgSalary), `Company.sector`, `Company.subsector`, `Company.entityType`, `User.pains[]`, `User.goals[]`.
- R1.2: Zod schema + `authStore` — قبول الحقول الجديدة (اختيارية للتوافق العكسي).
- R1.3: `/onboarding` — 3 شرائح إضافية (Identity+OPEX / Pains / Goals) بين ما هو موجود.

### المرحلة R2 — Handoff من التشخيص (1 commit)
- R2.1: بعد نتيجة التشخيص العميق، زر «ابدأ التسلسل الاستراتيجي» يبني payload ويحفظه في `proClientStore` ثم `navigate('/manager/clients/:id/journey')`.

### المرحلة R3 — بوّابة الأهداف (1 commit)
- R3.1: `client/src/lib/goalGating.ts` — دالة نقيّة `visibleTools(goals: string[]): ToolId[]`.
- R3.2: `ClientDetailPage` + `nav.ts` — قناع بصري (شبه مطموس) للأدوات خارج الأهداف المختارة، مع إبقاء الوصول للـ URL مباشر.

### المرحلة R4 — تغذية OPEX (5 commits على 5 أدوات)
- R4.1: GapAnalysis — قراءة `opex.target` كافتراضي لكل محور.
- R4.2: KPIs — بذر بذور KPIs من الميزانية والمستهدف.
- R4.3: AnsoffMatrix — تغذية معدلات النمو من OPEX.
- R4.4: FinancialAnalysis — تغذية بنود Dupont/Monte Carlo.
- R4.5: (لاحق) RACI عندما يُبنى.

### المرحلة R5 — القفل والتسلسل المرئي (3 commits)
- R5.1: `client/src/lib/journeyStages.ts` — تعريف الـ4 مراحل المقفلة و2 المفتوحة.
- R5.2: `<JourneyProgressBar>` + `<StageLock>` — component يمنع الدخول قبل استكمال السابقة.
- R5.3: صفحة `/manager/clients/:id/journey` — لوحة مرحلية تُظهر التقدّم وتفتح الأدوات المسموحة.

### المرحلة R6 — الأدوات الأربع الأساسية المفقودة (4 commits)
- R6.1: `BusinessModelCanvasPage.tsx` — 9 كتل، حفظ artifact `BMC`.
- R6.2: `BSCPage.tsx` — 4 أبعاد BSC.
- R6.3: `RACIPage.tsx` — مصفوفة المسؤوليات (تعتمد على `opex.team`).
- R6.4: `EisenhowerPage.tsx` — أساس Pains.

### المرحلة R7 — الأدوات الستّ الثانوية (تأجيل)
CustomerJourney، DigitalReadiness، StressTest، StrategyMap، Pareto، InternalEnvironment (ربما تُدمج في `/deep-analysis` القائم). كل واحدة PR منفصل عند الطلب.

### المرحلة R8 — تصليب (3 commits)
- R8.1: Vitest — `goalGating`, `journeyStages`, `stageLock`.
- R8.2: توسيع `scripts/smoke.sh` — 3 اختبارات جديدة (POST /onboarding مع OPEX، GET journey stage 1، افتح مغلق → 403).
- R8.3: توثيق `PRO-INDEPENDENT-FINAL-for-AI.md` (طلب المستخدم).

---

## القسم ز — تقدير الجهد الكلّي

| المرحلة | Commits | جهد تقديري |
|---------|---------|-------------|
| R1 توسيع التسجيل | 3 | جلسة قصيرة |
| R2 Handoff | 1 | جلسة قصيرة |
| R3 Goal-gating | 2 | جلسة قصيرة |
| R4 OPEX feeding | 5 | جلسة متوسّطة |
| R5 القفل | 3 | جلسة متوسّطة |
| R6 الأربع الأساسية | 4 | جلسة كبيرة |
| R7 الست الثانوية | 6 | مؤجّل |
| R8 تصليب | 3 | جلسة قصيرة |
| **الإجمالي (R1→R6+R8)** | **21 commit** | ~جلستان متوسّطتان |

---

## القسم ح — التوصية

- **لا نُوقف** الجزء ٢ من `resilient-crafting-stardust.md`. الأمر ٢٧ (تحقّق الجزء ١) يمرّ ثم نُنفّذ الأوامر ٢٨→٤٥ كما هي.
- **بعد الأمر ٤٥**، نبدأ R1→R6+R8 بالترتيب أعلاه. تجنّب R7 حتى طلب صريح.
- **قبل R1**، نحتاج إجابة على القرارات هـ.1..هـ.4 لتثبيت النطاق.
- **بعد اعتماد النطاق**، يُضاف مقتطف من هذا الملف في رأس `PRO-INDEPENDENT-FINAL-for-AI.md` كقسم «طبقة الربط».

---

**قبل التنفيذ ننتظر:**
1. اعتماد التوصية أعلاه.
2. الإجابة على القرارات هـ.1..هـ.4.
3. تأكيد أن الجزء ٢ من الخطة القائمة يُكمَل قبل بدء R1.
